name: Android Debug Build

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  build-debug-apk:
    name: ডিবাগ APK বিল্ড করুন
    runs-on: ubuntu-latest

    steps:
    - name: কোড চেকআউট করুন
      uses: actions/checkout@v4

    - name: JDK 8 সেটআপ করুন
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'temurin'

    - name: Android SDK এবং build-tools সেটআপ করুন
      uses: android-actions/setup-android@v2  # v2 ব্যবহার করুন

    - name: প্রয়োজনীয় Android packages ইনস্টল করুন
      run: |
        echo "y" | sdkmanager "platform-tools" "platforms;android-33" "build-tools;33.0.2"
        sdkmanager --licenses

    - name: Gradle ক্যাশে করুন
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: gradlew এক্সিকিউটেবল করুন
      run: chmod +x ./gradlew

    - name: ডিবাগ APK বিল্ড করুন
      run: ./gradlew assembleDebug --no-daemon

    - name: APK আপলোড করুন
      uses: actions/upload-artifact@v4
      with:
        name: debug-apk
        path: app/build/outputs/apk/debug/*.apk
        if-no-files-found: error

    - name: বিল্ড রিপোর্ট আপলোড করুন
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: build-reports
        path: app/build/reports/
        if-no-files-found: ignore